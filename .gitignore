node_modules/
cache/
logs/
data/
.env
# real (private) region config — only the .example is committed
config/region-lists.json
# browser profiles if ever placed inside the repo
profile/
profiles/
# per-user LINE bot trees (Google sessions + friend PII)
users/
# local business research — this repo is public and these are competitor
# analysis and monetization notes, so they stay out of it deliberately
docs/research/
